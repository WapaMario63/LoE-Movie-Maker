#ifndef LIB_H
#define LIB_H

/* This file is only used for when this program is compiled as a static library
   the main function is excluded and everything in it is inside the main function
   in this class.

   This is still a work in progress.

   Reason why I am doing this is because the entirety of this server will be a
   library used by the [LoEWCT] Launcher and for the plugin API.
*/

/*
#define IS_LIB false
#define IS_APP true

#include "widget.h"

class lib
{
public:
    lib();
    ~lib();
    void main();
};
*/
#endif // LIB_H
